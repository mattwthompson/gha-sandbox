name: Variables

on:
  workflow_dispatch:

jobs:
  test:
    name: event conditionals
    runs-on: ubuntu-latest
    steps:
      - name: Printing env
        run: |
            if [[ "$GITHUB_EVENT_NAME" == "push" ]]; then
              echo "caught"
            fi
